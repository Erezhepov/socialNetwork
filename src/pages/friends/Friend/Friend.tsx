import React from 'react';
import {IUserPage} from "../../users/user/User";


const Friend: React.FC<IUserPage> = (props) => {
    return (
        <div>

        </div>
    );
};

export default Friend;